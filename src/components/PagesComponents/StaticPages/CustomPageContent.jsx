"use client"
import CustomPage from '@/components/StaticPages/CustomPage'

const CustomPageContent = ({ slug }) => {
  return (
    <><CustomPage slug={slug} /></>
  )
}

export default CustomPageContent
